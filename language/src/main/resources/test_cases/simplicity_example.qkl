*--Variable is declared by first specifying the name, then the type, then the value
a: num = 5;

*-- Function is declared similarly to a variable by stating the parameters first, then the function name, then the body
pass (a: num, b: num) to add: num -> a + b;

**--
-- 'Run' is used to specify that there is a block of code
-- 'Catch' can be appended to all 'Run' blocks to catch any errors that occur
--**
run {
} catch(e) {
}

**--
-- For Loops are Defined using the 'run' keyword, followed by the initialization,
-- condition, and incrementation, then the body and an optional catch block
--**
run (x: num = 5; ?(x < 10); ++x) {
} catch(e) {
}

**--
-- While Loops are defined using the 'run' keyword, followed by the condition
-- then the body and an optional catch block
--**
run ?(a < b) {
} catch(e) {
}

**--
-- Do While Loops are defined using the 'run' keyword, followed by the body
-- then the condition and an optional catch block
--**
run {
} ?(a < b) catch(e) {
}

**--
-- If Statements are special types of blocks that are used to control the flow of the program
-- If statements are defined using the 'if' keyword, followed by the condition
-- then the body and an optional 'else if' or 'else' block
--**
if ?(a == 10) {
} else if ?(a == 20) {
} else {
}

